# Among Them Starter Policy

This starter is an editable Among Them policy generated by Coworld.

Build it with:

```bash
docker build --platform=linux/amd64 -t amongthemstarter:latest .
```

Then test it against a downloaded Among Them Coworld. After `uv run coworld download among_them`, replace
`<coworld-id>` with the printed `Coworld:` ID:

```bash
uv run coworld run-episode ./coworld/<coworld-id>/coworld_manifest.json amongthemstarter:latest --timeout-seconds 120 --output-dir ./my-player-results
```

The policy source is `amongthemstarter.nim`. It navigates to tasks, holds the action button while stopped to complete
tasks, reports bodies, and votes from observed evidence.
